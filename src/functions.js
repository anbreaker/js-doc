/**
 * Add two number
 * @param {Number} n1 first param
 * @param {Number} n2 second param
 * @returns {Number} Total result
 */
const add = (n1, n2) => {
  return n1 + n2
}

/**
 * Add two number and returns a string
 * @param {Number} n1 first number
 * @param {Number} n2 second number
 * @returns {String} Total result
 */
const addTwo = (n1, n2) => `Result is: ${n1 + n2}`
